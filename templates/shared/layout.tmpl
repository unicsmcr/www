{{ define "layout" }}
  <!doctype html>
  <html>
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <meta name="description" content="Student-led tech society based in Manchester." />
      <meta name="robots" content="index,follow" />
      <meta name="og:image" content="assets/images/fb.png" />
      <meta name="og:image:width" content="300" />
      <meta name="og:image:height" content="300" />
      <meta name="og:type" content="website" />
      <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Open+Sans:400,300" />
      <link rel="stylesheet" type="text/css" href="assets/css/layout.min.css" />
      {{ template "head" . }}
    </head>
    <body>
      {{ template "header" . }}
      {{ template "content" . }}
      {{ template "footer" . }}
      {{ template "scripts" . }}
    </body>
  </html>
{{ end }}

// Sets default values for optional blocks.
{{ define "scripts" }}{{ end }}